1.该页故障由几号进程引发？(2)
2.在该故障发生前，该进程执行的最后一个可执行文件是什么？(/bin/sh)该可执行文件的未初始化数据段（BSS）的起始虚拟地址（VMA）是什么？(0x00032000)
3.引发该页故障的线性地址是什么？(0x8032000)
4.该页故障处理前，该线性地址对应的页表项的地址和值分别是什么？(0xff70c8)(0x00000000)
5.该页故障处理后，上述线性地址对应的页表项的值是什么？(0x00ff2007)该线性地址被映射到的零页（新页帧）的起始地址是什么？(0x00ff2000)
6.该新页帧的头16个字节是什么？(00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00)